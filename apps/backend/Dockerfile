# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=8000
ENV NODE_ENV=production

ENV DB_NAME=resnity

# Auth0
ENV AUTH0_AUDIENCE=http://localhost:8000/api
ENV AUTH0_CLIENT_ID=C1UmfzVUmJOBMiqnSE8A9LXB1mRvPXJ3
ENV AUTH0_DOMAIN=tat-smart-developer.jp.auth0.com

# Restaurant
ENV RESTAURANT_DB_COLLECTION=restaurants

# Menu
ENV MENU_DB_COLLECTION=menus

WORKDIR /app

RUN addgroup --system backend && \
          adduser --system -G backend backend

COPY dist/apps/backend backend
RUN chown -R backend:backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix backend --omit=dev -f install

CMD [ "node", "backend" ]
